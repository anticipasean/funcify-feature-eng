

configurations {
    calcite {
        dependencies {
            implementation("org.apache.calcite:calcite-core:${apacheCalciteVersion}")
        }
    }
}

dependencies {
    implementation(project(':funcify-feature-eng-tools'))
}